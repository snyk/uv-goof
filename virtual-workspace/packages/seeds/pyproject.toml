[project]
name = "seeds"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["idna==3.6", "cryptography==3.3", "django==3.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
